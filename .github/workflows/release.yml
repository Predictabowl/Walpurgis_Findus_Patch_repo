name: Create Release

on:
  push:
    tags:
      - 'v*' # Push events to matching v*, i.e. v1.0, v20.15.10

jobs:
  build:
    name: Create Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create PK3 and Zip
        run: |
          # Create the pk3 file from the Walpurgis_Findus_Patch folder
          # pk3 files are essentially zip files with a different extension
          cd Walpurgis_Findus_Patch
          zip -r ../Walpurgis_Findus_Patch.pk3 .
          cd ..
          
          # Create the final zip containing README.txt and the .pk3 file
          zip release.zip README.txt Walpurgis_Findus_Patch.pk3

      - name: Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: release.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
